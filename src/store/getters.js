export const getters = {
  apiKey(state) {
    return state.apiKey;
  },
  config(state) {
    return state.config;
  },
  genres(state) {
    return state.genres;
  },
  similarMovies(state) {
    return state.similarMovies;
  },
};
